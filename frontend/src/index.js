// Import necessary dependencies
import React from "react"
import ReactDOM from "react-dom"
import App from "./App" // Assuming App.js contains your main React component

// Render the main component (App) into the root element of the HTML document
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root"),
)
